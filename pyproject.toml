[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hu-mind-ai"
version = "0.1.0"
description = "A creative proposal and independent logic-review pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["humind*"]
